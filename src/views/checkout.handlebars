<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Compra Exitosa</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.1.2/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 font-sans text-gray-800">

    <!-- Contenedor principal -->
    <div class="relative container mx-auto p-6 bg-white rounded-lg shadow-lg mt-10 max-w-xl">

        <!-- Título de la compra exitosa -->
        <h1 class="text-3xl font-bold text-center text-green-500 mb-4">Compra Exitosa</h1>

        <h2 class="text-2xl font-semibold text-gray-700 mb-4">Detalles de tu Compra</h2>

        <!-- Detalles de la compra -->
        <div class="space-y-4">
            <p><span class="font-bold">Código de compra:</span> {{ticket.code}}</p>
            <p><span class="font-bold">Fecha de compra:</span> {{ticket.purchase_datetime}}</p>
            <p><span class="font-bold">Monto total:</span> ${{ticket.amount}}</p>
            <p><span class="font-bold">Comprador:</span> {{ticket.purchaser}}</p>
        </div>

        <!-- Botón para volver al carrito dentro de la card -->
        <div class="mt-6 text-center">
            <a href="/cart/view" class="bg-blue-500 text-white py-2 px-4 rounded-lg hover:bg-blue-600 transition duration-200 w-full">
                Volver al carrito
            </a>
        </div>
    </div>

    <!-- Botón de cerrar sesión fuera de la card, arriba a la derecha -->
    <form id="logoutForm" action="/api/sessions/logout" method="post" class="absolute top-4 right-4">
    <button type="submit" class="bg-red-500 text-white py-2 px-4 rounded-lg hover:bg-red-600 transition duration-200">
      Cerrar sesión
    </button>
  </form>

    <!-- Botón para volver a los productos fuera de la card, arriba a la derecha -->
    <a href="/" class="absolute top-4 right-32 bg-gray-500 text-white py-2 px-4 rounded-lg hover:bg-gray-600 transition duration-200">
        Volver a los productos
    </a>

</body>
</html>




